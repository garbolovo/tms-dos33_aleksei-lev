                          ┌──────────────────────┐
                          │        HOST           │
                          │ (рабочая станция)     │
                          └─────────┬────────────┘
                                    │
                                    │ SSH только сюда:
                                    │ 127.0.0.1:2222  →  JumpHost:22
                                    │ (port-forward NAT)
                                    │
                          ┌─────────▼────────────┐
                          │      VirtualBox       │
                          └─────────┬────────────┘
                                    │
            ┌───────────────────────┴────────────────────────┐
            │                                                │
            │     NAT network (10.0.2.0/24)                  │
            │ (интернет + админ-канал, но наружу открыт       │
            │   только JumpHost через port-forward)            │
            │                                                │
            │  JumpHost: 10.0.2.15     vm1: 10.0.2.16          │
            │          (пример)              (пример)         │
            │  vm2: 10.0.2.17         vm3: 10.0.2.18          │
            │                                                │
            └───────────────────────┬────────────────────────┘
                                    │
                                    │
            ┌───────────────────────┴────────────────────────┐
            │                                                │
            │   Internal Network "lab-net" (10.10.0.0/24)     │
            │        (рабочая сеть VM ↔ VM, хост не видит)     │
            │                                                │
            │   JumpHost  ──────────── vm1 ─────────── vm2     │
            │   10.10.0.100           10.10.0.10      10.10.0.11
            │                    \                     /
            │                     \                   /
            │                      └────── vm3 ──────┘
            │                             10.10.0.20
            │                           (NTP server)
            │                                                │
            └────────────────────────────────────────────────┘




Используем 2 адаптера, но вместо Host-only:
Adapter 1 = Internal Network (рабочая сеть между VM) - статику 10.10.0.0/24
Adapter 2 = NAT (служебный “админский” доступ) - у каждой VM будет 10.0.2.x/24 (дефолтный NAT)
но внешний доступ с HOST делаем ТОЛЬКО на JumpHost через проброс порта (port-forward)
остальные VM по SSH с HOST будут недоступны → только через JumpHost → условие задания выполняется

enp0s3  → NAT → 10.0.2.15
enp0s8  → Internal → 10.10.0.x


                        (ssh :2222)
Mac
 |
 |  NAT (10.0.2.0/24)
 |
[JumpHost]
 | enp0s3 (NAT)      ← служебный
 | enp0s8 (10.10.0.100)
 |
 +------------------- Internal Network (lab-net) ------------------+
 |                                                                 |
 | 10.10.0.10        10.10.0.11           10.10.0.20               |
[VM1]               [VM2]                [VM3/NTP]


На каждой VM — 2 сетевых адаптера:
***Adapter 1 — Internal Network (lab-net)***
VM ↔ VM
хост (Mac) не видит
***Adapter 2 — NAT***
10.0.2.x/24 (автоматически)
интернет (apt)
доступ с Mac только через port-forward на JumpHost


ssh client (Mac)
   │
   │  TCP 127.0.0.1:2222   ← loopback ТОЛЬКО на Mac
   │
   ▼
VirtualBox NAT engine
   │   (port forwarding)
   │   127.0.0.1:2222  →  JumpHost:22
   │
   ▼
JumpHost VM (sshd, порт 22)
